const base = `
mx-auto
w-full
px-6
`;

export const containerSizes = {
  sm: `${base} max-w-3xl`,
  md: `${base} max-w-5xl`,
  lg: `${base} max-w-6xl`,
  xl: `${base} max-w-7xl`,
  full: `${base} max-w-full`,
};
